# GitHub Personal Access Token (classic) with `repo` + `read:user` scopes.
# Create at: https://github.com/settings/tokens
# In Vercel, add this as an Environment Variable named GH_TOKEN (do NOT commit a real token).
GH_TOKEN=ghp_your_token_here
